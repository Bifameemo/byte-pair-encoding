# TODO: only count non-overlapping pairs
# TODO: make Token directly reference Token (not int as index into table)
# TODO: add support for files passed as arguments through CLI
# TODO: recursively retry finding next_tokens with using last_token.right
# TODO: construct a dictionary which maps tokens to tokens which start with that token
# TODO: construct a dictionary which measures frequency of each token within original text
# TODO: choose next word weighted by frequency in original text
